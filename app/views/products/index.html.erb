<br>
<br>
<p><a href="/products/random">Random Product</a></p>
<p>Sort Price: <a href="/products?sort=price&sort_order=asc">Low</a> <a href="/products?desc=price&sort_order=desc">High</a></p>
<br>
<br>
<div class="container">
  <div class="row">
    <% @products.each do |product| %>

      <div class="col-sm-4 col-sm-offset-2">
        <div class="card" style="width: 15rem;">
        <img class="card-img-top" src="<%= product.images.first.url %>" alt="Card image cap">
        <div class="card-body">
          <h4 class="card-title"><%= product.name %></h4>
          <p class="card-text">$ <%= product.price %></p>
          <a href="/products/<%= product.id %>" class="btn btn-primary">VIEW</a>
          <a class="btn btn-primary">BUY</a>
        </div>
      </div>
    </div>
    <br><br>
    <% end %>
  </div>
</div>
